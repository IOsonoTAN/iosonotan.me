import React from 'react'

const Loading = ({ classes = '' }) => {
  return (
    <div id="loading-text" className={classes}>
      <h1>IO<small>sono</small>TAN</h1>
    </div>
  )
}

export default Loading
